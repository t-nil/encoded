/// This wil contain logic for manually inspecting encodes. Think of it as
/// syncing media players up, so videos can be inspected side-by-side, or
/// extracting single frames from every encode and loop through them with
/// various speeds (so as to see the decay in quality e.g.)
///
/// TODO also needs a mode (somwhere) to generate and compare encodes with
/// different settings that have the same bitrate (just use VBR?)
pub struct _FooBar;
